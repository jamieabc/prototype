This repository is for web prototyping. It auto compiles scss, triggers reload when html/css changes.

### Commands
1. Install all pacakges

    `npm install`

1. Start server and watch files

    `npm run watch`
